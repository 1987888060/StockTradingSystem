<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jsu.per.system.dao.RoleMapper">
    <select id="findRight" parameterType="int" resultType="Integer">
        SELECT right_id FROM `role_right` where role_id = #{id}
    </select>

</mapper>